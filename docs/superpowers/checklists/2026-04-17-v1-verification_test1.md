# Flikky v1 人工验收清单

> 运行前：确保手机与电脑处于同一 WiFi；手机已赋予通知权限。

## 基本功能

- [正确] 点"启动服务"后，ServingScreen 显示 URL 和 6 位 PIN
- [无效果] 通知栏出现常驻通知，显示 URL 和 PIN
- [错误：不是 MD3 风格] 电脑 Chrome 访问 URL，看到 MD3 风格登录页（紫色主题、分格 PIN 输入）
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 输错 PIN 3 次：页面提示"尝试过多，30 秒后再试"
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 输对 PIN：跳转到聊天页；手机端"等待连接…"变"已连接"
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 电脑发文本"hello"：手机消息列表出现；浏览器气泡为右侧紫色
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 手机端输入消息并点发送：浏览器收到；手机气泡视觉上区分（origin=PHONE）
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 电脑上传 1 MB 文件：上传成功，手机消息列表多出一条文件消息，APP 底部文件计数 +1
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 手机点"附件"选文件：浏览器收到文件提示气泡，点击可下载
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 浏览器顶部状态：uptime 每秒 +1、fileCount 正确、rate 非零
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 点"停止服务"：通知栏消失；浏览器 WebSocket `已断开`、重试失败
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 重启服务后 PIN 变新值；旧浏览器 Cookie 失效，重新走登录

## 安全

- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 同网段但 IP 不同的另一台设备能访问（可接入）
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 不同 WiFi 的设备无法 ping 通服务 IP:Port
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 输错 PIN 5 次（包含锁定期间的尝试）：服务终止，再输正确 PIN 也被拒
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 隐私/无痕窗口访问功能一切正常（登录 + 聊天 + 上传）
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 响应头包含 `Content-Security-Policy`、`X-Frame-Options: DENY`（DevTools Network 面板验证）
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] `Set-Cookie: flikky_token` 含 `HttpOnly`、`SameSite=Strict`

## 稳定性

- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] APP 切后台 10 分钟：服务仍运行（前台服务保活）
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] WiFi 中途切换：浏览器 `已断开`（v1 不自动重绑，v1.1 迭代）
- [输入正确的PIN点击进入依然刷新为当前页面，所以该项未能成功测试] 大文件 100 MB 上传：完成、APP 不 OOM

记录验证日期与设备：4-17-test1-Android15
